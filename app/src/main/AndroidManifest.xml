<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tan.iotwfirebase">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".welcom_page"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Control"
            android:screenOrientation="portrait">
        </activity>

        <activity
            android:name=".add_sensor"
            android:screenOrientation="portrait">
        </activity>

        <activity
            android:name=".sensor_select"
            android:screenOrientation="portrait">
        </activity>

        <activity
            android:name=".RemoveSensor"
            android:screenOrientation="portrait">
        </activity>

        <activity
            android:name=".RemoveGroup"
            android:screenOrientation="portrait">
        </activity>

        <service
            android:name=".service.LocationService"
            android:enabled="true"
            android:exported="true" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!--<activity android:name=".RemoveGroup" />-->
        <!--<activity android:name=".Control" />-->
        <!--<activity android:name=".add_sensor" />-->
        <!--<activity android:name=".sensor_select" />-->
        <activity android:name=".generalGraph"
            android:screenOrientation="portrait"/>
        <!--<activity android:name=".RemoveSensor" />-->
    </application>

</manifest>